function onCreate()
    if getProperty('sensitive.song') then
        debugPrint('but you can go ahead and delete the events.json in data/sensitive if you want i dont mind');
        debugPrint('This song may have some shitty ahh camera zooming affect');
    end
end